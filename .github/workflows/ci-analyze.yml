name: CI-Analyze

on:
  push:
    paths-ignore: [man/**, conf/**, docs/**, README.md, LICENSE, .gitignore, rust-rudo.spec]
  pull_request:
    paths-ignore: [man/**, conf/**, docs/**, README.md, LICENSE, .gitignore, rust-rudo.spec]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: full

jobs:
  code-analyze:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-20.04, macos-10.15 ]
    steps:
      - uses: actions/checkout@v2.3.4
      - name: Update CI
        if: matrix.os == 'ubuntu-20.04'
        run: sudo apt update
      - name: Install dependency
        if: matrix.os == 'ubuntu-20.04'
        run: sudo apt install libpam0g-dev libsystemd-dev
      - name: cargo clippy
        run: cargo clippy --all-targets --all-features --verbose
        #- uses: actions-rs/clippy-check@v1
        #with:
        #token: ${{ secrets.GITHUB_TOKEN }}
        #args: --all-features --all-targets --verbose
