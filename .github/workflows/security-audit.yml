name: Audit

on:
  schedule:
  - cron: "0 2 * * 1-5"

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Audit dependency
      run: cargo audit
